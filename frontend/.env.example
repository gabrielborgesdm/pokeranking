# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# NextAuth Configuration
# NEXTAUTH_SECRET is used by NextAuth to encrypt the session cookie stored in the browser.
# This is separate from the backend JWT - NextAuth wraps your backend's access_token
# in its own encrypted session for secure client-side storage.
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-change-in-production
NEXTAUTH_URL=http://localhost:3000

# Contribute Page Configuration (all optional)
NEXT_PUBLIC_GITHUB_URL=https://github.com/your-username/your-repo

# Stripe Configuration (for embedded checkout with Card + Pix)
# Get keys from: https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
STRIPE_SECRET_KEY=sk_test_your_secret_key
# Create a price in Stripe Dashboard: Products > Add Product > Set "customer chooses price"
NEXT_PUBLIC_STRIPE_PRICE_ID=price_your_price_id

# Google Analytics (optional)
# Get your Measurement ID from: https://analytics.google.com > Admin > Data Streams
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

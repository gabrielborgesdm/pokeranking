# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000

# NextAuth Configuration
# NEXTAUTH_SECRET is used by NextAuth to encrypt the session cookie stored in the browser.
# This is separate from the backend JWT - NextAuth wraps your backend's access_token
# in its own encrypted session for secure client-side storage.
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-change-in-production
NEXTAUTH_URL=http://localhost:3000
